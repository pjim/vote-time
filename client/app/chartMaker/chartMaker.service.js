'use strict';

angular.module('voteTimeApp')
  .service('chartMaker', function () {
    // AngularJS will instantiate a singleton by calling "new" on this function
    // this service takes an array response from the server strips polls with no votes
    // then returns an array of highchart object  s
  });
