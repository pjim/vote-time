'use strict';

angular.module('voteTimeApp')
  .service('votedChecker', function (Auth) {
    // AngularJS will instantiate a singleton by calling "new" on this function
    // this service should take in a value then check auth name against these voted values
    // then remove any that they've voted on and return the rest in an array
    
  });
